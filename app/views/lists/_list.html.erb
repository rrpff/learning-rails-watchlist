<ul class="film-list">
    <% for film in list.films.order('title ASC') %>
        <%= render partial: 'films/film', locals: { film: film, list: list } %>
    <% end %>
</ul>

<%= render partial: 'lists/form', locals: { list: list } %>